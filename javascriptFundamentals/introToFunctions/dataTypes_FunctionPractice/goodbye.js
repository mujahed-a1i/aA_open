// Write a function goodbye(name) that takes in a string name and returns a string saying "bye" to that name. 

function goodbye (name){ 
    return 'Bye ' + name
}

console.log(goodbye("Mujahed"));

function hello(str) {
    let str1 = "Hello, " + str;
    return str1;
  }
  
